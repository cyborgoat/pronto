{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Pronto windows",
  "windows": ["main", "launcher"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default"
  ]
}
